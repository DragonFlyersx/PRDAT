Main ::= Expr EOF rule A
Expr ::= NAME rule B
    | CSTINT rule C
    | MINUS CSTINT rule D
    | LPAR Expr RPAR rule E
    | LET NAME EQ Expr IN Expr END rule F
    | Expr TIMES Expr rule G
    | Expr PLUS Expr rule H
    | Expr MINUS Expr rule I
    
Right most derivation of the input: let z = (17) in z + 2 * 3 end EOF 

Definition of right most derivation: 
A rightmost derivation is a sequence of production rules applied to the start symbol, 
where at each step, the rightmost non-terminal symbol is replaced by one of its production rules.


Main 
=> Expr EOF (rule A)
=> LET NAME EQ Expr IN Expr END EOF (rule F)
=> LET NAME EQ Expr IN Expr TIMES Expr END EOF (rule G)
=> LET NAME EQ Expr IN Expr TIMES CSTINT END EOF (rule C)
=> LET NAME EQ Expr IN Expr PLUS Expr TIMES CSTINT END EOF (rule H)
=> LET NAME EQ Expr IN Expr PLUS CSTINT TIMES CSTINT END EOF (rule C)
=> LET NAME EQ Expr IN NAME PLUS CSTINT TIMES CSTINT END EOF (rule B)
=> LET NAME EQ LPAR Expr RPAR IN NAME PLUS CSTINT TIMES CSTINT END EOF (rule E)
=> LET NAME EQ LPAR CSTINT RPAR IN NAME PLUS CSTINT TIMES CSTINT END EOF (rule C)
With Terminal applied
=> LET z EQ LPAR 17 RPAR IN z PLUS 2 TIMES 3 END EOF

done with left most derivation of the input: let z = (17) in z + 2 * 3 end EOF:

Main
=> Expr EOF (rule A)
=> LET NAME EQ Expr IN Expr END EOF (rule F)
=> LET NAME EQ LPAR Expr RPAR IN Expr END EOF (rule E)
=> LET NAME EQ LPAR CSTINT RPAR IN Expr END EOF (rule C)
=> LET NAME EQ LPAR CSTINT RPAR IN Expr PLUS Expr END EOF (rule H)
=> LET NAME EQ LPAR CSTINT RPAR IN Expr PLUS CSTINT END EOF (rule C)
=> LET NAME EQ LPAR CSTINT RPAR IN NAME PLUS CSTINT END EOF (rule B)
=> LET NAME EQ LPAR CSTINT RPAR IN NAME PLUS CSTINT TIMES Expr END EOF (rule G)
=> LET NAME EQ LPAR CSTINT RPAR IN NAME PLUS CSTINT TIMES CSTINT END EOF (rule C)
With Terminal applied
=> LET z EQ LPAR 17 RPAR IN z PLUS 2 TIMES 3 END EOF


  
  
  S -> SS
  S -> aSb
  s -> epsilon
  
  Højre afledning:
    s => S(S) => SaSb => Sab => ab
  Venstre afledning:
    s => (S)S => (aSb)S => (ab)S => (ab)(ab) => abab
  
  
  NON TERMINAL SYMBOLS:
    S
  TERMINAL SYMBOLS: 
    a, b
    START SYMBOL:
    s